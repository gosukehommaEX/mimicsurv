% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simPE.R
\name{simPE}
\alias{simPE}
\title{Simulate Survival Data from Piecewise Exponential Distribution}
\usage{
simPE(n, time_points, hazard_rates, max_time, censoring_prob = 0.1)
}
\arguments{
\item{n}{Sample size (number of subjects to simulate)}

\item{time_points}{Numeric vector of time points defining intervals}

\item{hazard_rates}{Numeric vector of hazard rates for each interval}

\item{max_time}{Maximum follow-up time (administrative censoring)}

\item{censoring_prob}{Probability of random censoring (default 0.1)}
}
\value{
Data frame with columns:
  \item{time}{Observed time (either event time or censoring time)}
  \item{status}{Event indicator (1 = event, 0 = censored)}
}
\description{
This function generates simulated survival data following a piecewise
exponential distribution with specified time intervals and hazard rates.
}
\details{
The function simulates survival times using inverse transform sampling
for piecewise exponential distribution. Random censoring is applied
uniformly between 0 and the simulated survival time.
}
\examples{
# Simulate data with 4 intervals
time_points <- c(0, 6, 12, 18, 24)
hazard_rates <- c(0.05, 0.08, 0.12, 0.15)

sim_data <- simPE(
  n = 200,
  time_points = time_points,
  hazard_rates = hazard_rates,
  max_time = 24,
  censoring_prob = 0.15
)

# Check event rate
table(sim_data$status)

}
